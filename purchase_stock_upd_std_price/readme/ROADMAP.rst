* For storable products, stock valuation layer (and also, stock journal entries
  when Inventory Valuation is set to auto) is created before cost is changed, so
  created with previous product price.
* Improve product price update when moves are validated.
