<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_purchase_order_form_inherit" model="ir.ui.view">
        <field name="name">purchase.order (FCD Purchase Order)</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form" />
        <field name="arch" type="xml">
            <xpath expr="//form" position="attributes">
                <attribute name="duplicate">0</attribute>
            </xpath>
            <xpath expr="//tree/field[@name='name']" position="after">
                <field name="fcd_lot_id" widget="many2onebutton" />
            </xpath>
            <xpath expr="//tree/field[@name='price_subtotal']" position="after">
                <field name="fcd_lot_finished" />
            </xpath>
            <xpath expr="//page[@name='purchase_delivery_invoice']/group/group/field[@name='picking_type_id']" position="attributes">
                <attribute name="invisible">True</attribute>
            </xpath>
            <xpath expr="//field[@name='origin']" position="after">
                <field name="picking_type_id"/>
            </xpath>
            <!-- <xpath expr="//button[@name='action_rfq_send']" position="before">
                <button name="action_purchase_order_add_line" type="object" class="btn-primary" string="Add line"/>
            </xpath> -->
        </field>
    </record>

    <record id="view_purchase_order_form_inherit_secondary_unit" model="ir.ui.view">
        <field name="name">purchase.order Secondary Unit (FCD Purchase Order)</field>
        <field name="model">purchase.order</field>
        <field
            name="inherit_id"
            ref="purchase_order_secondary_unit.purchase_order_form"
        />
        <field name="arch" type="xml">
            <xpath expr="//form//field[@name='secondary_uom_id']" position="attributes">
                <attribute name="domain">[('product_tmpl_id.product_variant_ids', 'in', product_id and [product_id] or []), ('factor', '!=', 0)]</attribute>
            </xpath>
            <xpath expr="//tree//field[@name='secondary_uom_id']" position="attributes">
                <attribute name="domain">[('product_tmpl_id.product_variant_ids', 'in', product_id and [product_id] or []), ('factor', '!=', 0)]</attribute>
            </xpath>
        </field>
    </record>

</odoo>
