<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_purchase_order_confirm_receive" model="ir.actions.server">
        <field name="name">Confirm and receive</field>
        <field name="model_id" ref="model_purchase_order"/>
        <field name="binding_model_id" ref="purchase.model_purchase_order" />
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="code">
            action = model.action_batch_confirm_receive()
        </field> 
    </record>

    <record id="purchase_order_form" model="ir.ui.view">
        <field name="name">
            purchase.order.form (in purchase_order_confirm_receive)
        </field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form" />
        <field name="arch" type="xml">
            <button name="button_confirm" position="after">
                <button
                    name="button_confirm_receive"
                    type="object"
                    string="Confirm and receive"
                    class="oe_highlight"
                    attrs="{'invisible': [('state','not in',('draft','sent'))]}"
                />
            </button>
        </field>
    </record>

</odoo>
